[features]
codex_hooks = true

# Лимиты на суб-агентов. Собственных ролей у проекта больше нет (пять toml-ов
# в .agents/agents/ удалены: они дублировали скиллы tatarverse-* и держали
# захардкоженную версию модели). Секция остаётся, потому что этими лимитами
# пользуются агенты, которые приходят вместе со скиллом impeccable
# (.agents/skills/impeccable/agents/).
[agents]
max_threads = 4
max_depth = 1
