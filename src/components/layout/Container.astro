---
import { cn } from "@/lib/cn";

interface Props {
	class?: string;
	container?: boolean;
}

const { class: className, container } = Astro.props as Props;
---

<div
	class={!container
		? "not-container"
		: cn("container flex-1 px-5 mx-auto max-w-5xl", className)}
>
	<slot />
</div>
