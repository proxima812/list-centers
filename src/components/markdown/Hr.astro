---
import { cn } from "@/lib/cn";
import { markdownClasses } from "./markdownStyles";

interface Props {
	class?: string;
}

const { class: className } = Astro.props as Props;
---

<hr class={cn(markdownClasses.hr, className)} />
